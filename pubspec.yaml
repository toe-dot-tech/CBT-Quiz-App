name: cbt_quiz_app

description: A new Flutter project.

publish_to: 'none' # Remove this line if the package should be published

version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.0.2
  go_router: ^6.0.0
  sqflite: ^2.0.0+4
  http: ^0.14.0
  socket_io_client: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
